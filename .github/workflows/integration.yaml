name: Python package

on:
  push:
    branches:
      - '**'
      - '!main' 

jobs:
  check_pyproject:
    runs-on: ubuntu-latest
    outputs:
      pyproject_found: ${{ steps.check_pyproject.outputs.found }}
      directory: ${{ steps.check_directory.outputs.DIRECTORY }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check directory name
        id: check_directory
        run: |
          DIRECTORY=$(git log -1 --name-only --pretty=format: | awk -F'/' '{print $1}' | sort -u | head -n 1)
          export DIRECTORY=$(echo "${DIRECTORY}" | tr '_' '-')
          echo "DIRECTORY=${DIRECTORY}" >> $GITHUB_ENV
          echo "::set-output name=DIRECTORY::$DIRECTORY"

      - name: Check pyproject.toml in directory
        id: check_pyproject
        run: |
          if [ ! -f "${DIRECTORY}/pyproject.toml" ]; then
            echo "File pyproject.toml not found in ${DIRECTORY}. Skipping job."
            echo "::set-output name=found::false"
            exit 0
          else:
            echo "File pyproject.toml found."
            echo "::set-output name=found::true"
          fi

  integration:
    runs-on: ubuntu-latest
    needs: check_pyproject
    if: needs.check_pyproject.outputs.pyproject_found == 'true'

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install Poetry
      run: |
        curl -sSL https://install.python-poetry.org | python3 -
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Install dependencies
      run: |
        poetry install
      working-directory: ${{ needs.check_pyproject.outputs.DIRECTORY }}

    - name: Lint with Ruff
      run: |
        poetry run ruff check ${{ needs.check_pyproject.outputs.DIRECTORY }}
        poetry run ruff format --check ${{ needs.check_pyproject.outputs.DIRECTORY }}
      working-directory: ${{ needs.check_pyproject.outputs.DIRECTORY }}

    - name: Test with pytest
      run: |
        poetry run pytest tests
      working-directory: ${{ needs.check_pyproject.outputs.DIRECTORY }}

  bump-version:
    runs-on: ubuntu-latest
    needs: integration
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install Poetry
      run: |
        curl -sSL https://install.python-poetry.org | python3 -
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Install dependencies
      run: |
        poetry install
      working-directory: ${{ needs.check_pyproject.outputs.DIRECTORY }}

    - name: Bump version
      run: |
        poetry version patch
      working-directory: ${{ needs.check_pyproject.outputs.DIRECTORY }}

    - name: Commit changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add pyproject.toml
        git commit -m "Bump version" || echo "No changes to commit"
        git push
      working-directory: ${{ needs.check_pyproject.outputs.DIRECTORY }}
      env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
